#include <iostream>

using namespace std;

int suma(int a, int b)
{
    int d, s = 0;
    
    for(d = 1; d <= a and d <= b; d ++)
    {
        if(a % d == 0 and b % d == 0)
        {
            s += d;
        }
    }

    return s;
}
